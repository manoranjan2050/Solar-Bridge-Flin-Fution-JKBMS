# ── Secrets (NEVER commit) ──────────────────────────────────────────────────
# (gitignore does not support inline comments — keep patterns on their own line)
deploy_secrets.py
# config.ini holds live MQTT credentials on each machine:
config.ini

# ── Runtime data / generated ────────────────────────────────────────────────
energy.json
live_state.json
control_queue.json
*.db
*.db-wal
*.db-shm
automation.json
backup_*/

# ── Logs & captures ─────────────────────────────────────────────────────────
*.log
*_log.txt
*_out.txt
bridge_log.txt
dash_log.txt
solar_log.txt
diag_out.txt
frame_out.txt
passive_out.txt
dual_bms.txt

# ── Big / private assets ────────────────────────────────────────────────────
solarassisant/
*.img
*.img.xz
# personal notes — not for the public repo:
VIDEO_GUIDE.md

# ── pi-gen build (generated by pigen/build-os.sh) ───────────────────────────
pigen/stage-solar/01-copy-app/files/
# local debug screenshots:
bug/

# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
venv/
.venv/

# ── OS / editor ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/

# ── Claude Code local config (personal, do not publish) ──────────────────────
.claude/

# Flask session key (per-install secret)
.session_secret
